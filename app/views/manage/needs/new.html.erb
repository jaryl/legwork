<%= @need.errors.full_messages %>

<%= simple_form_for [:manage, @beneficiary, @need] do |form| %>
  <%= form.input :label_id %>
  <%= form.input :description %>

  <%= form.submit %>
<%- end %>

<%= link_to "Cancel", [:manage, @beneficiary] %>
